const UserController = require("../src/controllers/usersController")

const validateController = new UserController();

describe("UserController", () => {
  describe("validate SignUp", () => {
    test("should be able to signUp", () => {
      const user = {}
    })
  })
}
)

